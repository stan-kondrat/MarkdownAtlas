name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-arm64:
    runs-on: macos-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build arm64
      run: make build-arm64

    - name: Verify build
      run: |
        test -f build-arm64/MarkdownAtlas.app/Contents/MacOS/MarkdownAtlas
        file build-arm64/MarkdownAtlas.app/Contents/MacOS/MarkdownAtlas

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: MarkdownAtlas-arm64-${{ github.sha }}
        path: build-arm64/MarkdownAtlas.app
        retention-days: 30

  build-x86_64:
    runs-on: macos-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build x86_64
      run: make build-x86_64

    - name: Verify build
      run: |
        test -f build-x86_64/MarkdownAtlas.app/Contents/MacOS/MarkdownAtlas
        file build-x86_64/MarkdownAtlas.app/Contents/MacOS/MarkdownAtlas

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: MarkdownAtlas-x86_64-${{ github.sha }}
        path: build-x86_64/MarkdownAtlas.app
        retention-days: 30
